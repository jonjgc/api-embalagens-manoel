version: '3.8'
services:
  api-embalagens:
    build: .
    container_name: api-embalagens-manoel
    ports:
      - "3000:3000"
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    command: npm run start:dev